{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for all Teex editor windows",
  "windows": ["main", "teex-window-*"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
